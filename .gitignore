# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Recorded data, derived packs and trained models stay local: large, personal,
# and all reproducible from the scripts. Remove these lines to version them.
python/data/raw/
python/data/packs/
python/data/models/
python/data/downloads/
python/data/manifest.json

# Scanned source plan (2.3MB of images, no extractable text)
sign language plan.pdf

# Downloadable model assets - re-fetch with `python -m src.download_models`
web/public/models/*.task

# Node / web
node_modules/
web/dist/
.vite/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
